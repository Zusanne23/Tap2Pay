<Window x:Class="Tap2PaySystem.Views.TopUpHistoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Top Up History"
        Height="650"
        Width="1200"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back Button -->
        <Button x:Name="btnBack"
                Content="←"
                Width="40"
                Height="35"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                FontSize="18"
                FontWeight="Bold"
                Click="btnBack_Click"/>

        <!-- Title -->
        <TextBlock Text="Top Up History"
                   FontSize="26"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>

        <!-- Search -->
        <TextBox x:Name="txtSearch"
                 Grid.Row="1"
                 Width="300"
                 Height="35"
                 HorizontalAlignment="Left"
                 Margin="0,10,0,15"
                 VerticalContentAlignment="Center"
                 TextChanged="txtSearch_TextChanged"/>

        <!-- DataGrid -->
        <DataGrid x:Name="dgTopUpHistory"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">

            <DataGrid.Columns>

                <DataGridTextColumn Header="TopUp ID"
                                    Binding="{Binding TopUpId}"
                                    Width="100"/>

                <DataGridTextColumn Header="User ID"
                                    Binding="{Binding UserId}"
                                    Width="90"/>

                <DataGridTextColumn Header="Customer Name"
                                    Binding="{Binding FullName}"
                                    Width="220"/>

                <DataGridTextColumn Header="RFID UID"
                                    Binding="{Binding RFIDUID}"
                                    Width="170"/>

                <DataGridTextColumn Header="Amount"
                                    Binding="{Binding Amount,StringFormat=₱{0:N2}}"
                                    Width="120"/>

                <DataGridTextColumn Header="Top Up Date"
                                    Binding="{Binding TopUpDate}"
                                    Width="180"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>

</Window>